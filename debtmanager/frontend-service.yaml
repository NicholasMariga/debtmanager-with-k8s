apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: debtmanager
spec:
  selector:
    app: nginx-frontend
  ports:
  - protocol: TCP
    port: 9092
    targetPort: 80
  type: NodePort